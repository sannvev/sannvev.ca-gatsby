---
title: Home
description: Sannvev Institute is a Canadian non-profit advancing auditable AI and information integrity research.
---

import Layout from "../components/Layout";
import SEO from "../components/SEO";
import OrgJsonLd from "../components/OrgJsonLd";
import CardRow from "../components/CardRow";
import LogoSeal from "../components/LogoSeal";
import { useStaticQuery, graphql } from "gatsby";

export default ({ children }) => <Layout>{children}</Layout>;

export const Head = () => {
  const data = useStaticQuery(graphql`
    query { site { siteMetadata { siteUrl } } }
  `);
  return (
    <SEO pathname="/">
      <OrgJsonLd siteUrl={data.site.siteMetadata.siteUrl} />
    </SEO>
  );
};

<LogoSeal size="lg" />

# Sannvev Institute

A Canadian non-profit advancing **auditable AI** and **information integrity** research.

The Institute studies how trust, provenance, and verifiability can be engineered into information systems — from multi-agent AI to public discourse. Our work is grounded in formal epistemic reasoning and applied to domains where accountability matters: national security, regulated industries, and democratic infrastructure.

<CardRow cards={[
  { to: "/about/", title: "About", body: "Mission, directors, and governance of the Institute." },
  { to: "/research/", title: "Research", body: "Programs advancing auditable AI and information provenance." },
  { to: "/contact/", title: "Contact", body: "Reach the Institute for partnership, grant, and research inquiries." },
]} />

---

Member, Manitoba Innovates. Canadian federal not-for-profit corporation.
